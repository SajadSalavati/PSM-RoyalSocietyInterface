# Experiment: WSS field, Sten model, Wall region.
# This single file replaces the hand-edited constants of the original
# Code_*.ipynb notebook. Run with:
#   python -m psm_surrogate.cli --config configs/sten_wall_wss.yaml --stage all
name: sten_wall_wss
model: Sten
bc_name: Wall
variable_name: WSS

field_key: wss
npz_filename: "rylScty_patientSpec_StenosisModel_wallPre&OSI&WSS.npz"
n_snapshots: 1000

num_modes: 40

time_window: 100
test_split: 0.8

hidden_size: 512
num_layers: 3
dropout: 0.2
learning_rate: 1.0e-5
num_epochs: 200000
batch_size: 20
early_stop_patience: 100
