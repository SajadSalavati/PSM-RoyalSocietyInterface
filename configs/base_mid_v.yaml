# Experiment: v_y field, Base model, Mid region.
# This single file replaces the hand-edited constants of the original
# Code_*.ipynb notebook. Run with:
#   python -m psm_surrogate.cli --config configs/base_mid_v.yaml --stage all
name: base_mid_v
model: Base
bc_name: Mid
variable_name: v_y

field_key: v_y
npz_filename: "rylScty_patientSpec_BaseModel_miduvw.npz"
n_snapshots: 1000

num_modes: 15

time_window: 100
test_split: 0.8

hidden_size: 512
num_layers: 3
dropout: 0.2
learning_rate: 1.0e-5
num_epochs: 200000
batch_size: 20
early_stop_patience: 100
